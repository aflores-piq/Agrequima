# Solo se usa con "npm run dev" (servidor de Vite) -- Vite NO carga este
# archivo para "npm run build" (modo producción), así que no afecta el
# build que se despliega al servidor real. Ver src/api/client.ts.
VITE_API_URL=http://localhost:8001/api
